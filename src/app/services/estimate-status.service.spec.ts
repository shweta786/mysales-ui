/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { EstimateStatusService } from './estimate-status.service';

describe('Service: EstimateStatus', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [EstimateStatusService]
    });
  });

  // it('should ...', inject([EstimateStatusService], (service: EstimateStatusService) => {
  //   expect(service).toBeTruthy();
  // }));
});
